@model IEnumerable<BackOfficeRAM.Models.Imagem>

@{
    ViewBag.Title = "Index";
}



<script>
    $(document).ready(function () {
        $('#botaoImagens').addClass('active');
    });
</script>


<br />

@*<div class="row">
    <div class="col">*@
<p>
    @Html.ActionLink("Adicionar imagem", "Create")
</p>
@*</div>
        <div class="col">
            <input type="text" id="myInput" onkeyup="myFunction()" placeholder="Pesquisar..">
        </div>
    </div>*@


<table class="table table-hover" style="background-color:#fff">
    <tr>
        <th style="width:5%">Imagens</th>
        <th style="width:40%">Nome</th>
        <th style="width:30%">Autor</th>
        <th style="width:25%"></th>

    </tr>

    @foreach (var item in Model)
    {
        <tr style="cursor:pointer" onclick="javascript:window.location='@Url.Action("Details","Imagens", new{id = item.Id})'">
            <td>
                <img src="@item.ConteudoImagem" alt="@item.Nome" />
            </td>
            <td>
                @item.Nome
            </td>
            <td>
                @item.Autor
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Detalhes", "Details", new { id = item.Id }) |
                @Html.ActionLink("Eliminar", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
